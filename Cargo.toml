[package]
name = "loopwire"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "loopwire"

[build-dependencies]
bindgen = "0.70"
cc = "1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
